# Fixtures

These fixtures are used for parity testing between the current frontend behavior and the backend implementation.

- `graduate-payloads/*.json`: input payloads (valid + invalid) for submission endpoints.
- `dashboard/*.json`: sample graduate datasets plus expected aggregates.

